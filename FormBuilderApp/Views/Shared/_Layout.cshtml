<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Form Builder</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Form Builder", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @*<li>@Html.ActionLink("Home", "Index", "Home")</li>*@
                    @if (ViewContext.HttpContext.User.IsInRole("User"))
                    {
                        <li>@Html.ActionLink("Available Forms", "Forms", "User")</li>
                        <li>@Html.ActionLink("Submitted Forms", "Submitted", "User")</li>

                    }
                    @if (ViewContext.HttpContext.User.IsInRole("Super Admin") || ViewContext.HttpContext.User.IsInRole("Admin"))
                    {
                        <li>@Html.ActionLink("Create Form", "CreateForm", "Admin")</li>
                        <li>@Html.ActionLink("Create Users", "CreateUser", "Admin")</li>
                        <li>@Html.ActionLink("View Users", "Users", "Admin")</li>
                        <li>@Html.ActionLink("View Forms", "ViewFormsAdmin", "Admin")</li>
                    }
                    @if (ViewContext.HttpContext.User.IsInRole("Super User"))
                    {
                        <li>@Html.ActionLink("View Forms", "ViewFormsSuperUser", "SuperUser")</li>
                    }
                </ul> 
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Form Builder</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("JavaScript", required: false)
    @RenderSection("scripts", required: false)

</body>
</html>
